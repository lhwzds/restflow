# API Configuration
# Leave empty to use relative path (in development, proxied to localhost:3000 via vite.config.ts)
# In production, set to backend address, e.g.: https://api.restflow.ai
VITE_API_URL=

# Demo Mode (for frontend-only deployments, e.g., Vercel)
# Set to "true" to enable MSW mock data and display sample workflows
# Set to "false" to connect to real backend API
VITE_DEMO_MODE=false

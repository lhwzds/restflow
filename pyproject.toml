[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "restflow-ai"
version = "0.1.0"
description = "Python SDK for the RestFlow executable skill runtime."
requires-python = ">=3.11"
readme = "README.md"
license = "MIT"
authors = [
  { name = "RestFlow" }
]
keywords = [
  "agent",
  "skills",
  "rust",
  "python",
  "runtime"
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Rust",
  "Topic :: Software Development :: Libraries :: Python Modules",
  "Topic :: Software Development :: Build Tools"
]

[project.urls]
Homepage = "https://github.com/lhwzds/restflow"
Repository = "https://github.com/lhwzds/restflow"
Documentation = "https://docs.restflow.ai"

[tool.maturin]
bindings = "pyo3"
features = ["python-module"]
manifest-path = "crates/py/Cargo.toml"
module-name = "restflow.restflow_native"
python-source = "python"

[package]
name = "restflow-models"
version.workspace = true
authors.workspace = true
edition.workspace = true
description = "Shared model and provider primitives for RestFlow"

[dependencies]
restflow-traits = { workspace = true, features = ["specta"] }
serde = { workspace = true }
specta = { version = "=2.0.0-rc.23", features = ["derive", "serde_json"] }

[lints.rust]

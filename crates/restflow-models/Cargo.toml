[package]
name = "restflow-models"
version.workspace = true
authors.workspace = true
edition.workspace = true
description = "Shared model and provider primitives for RestFlow"

[dependencies]
restflow-traits = { workspace = true }

[lints.rust]
dead_code = "allow"
